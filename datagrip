#!/bin/bash
#Generated by JetBrains Toolbox 1.21.9712 at 2021-08-12T15:24:49.454276

declare -a ideargs=()
declare -- wait=""

for o in "$@"; do
  if [[ "$o" = "--wait" || "$o" = "-w" ]]; then
    wait="-W"
    o="--wait"
  fi
  if [[ "$o" =~ " " ]]; then
    ideargs+=("\"$o\"")
  else
    ideargs+=("$o")
  fi
done

open -na "/Users/tatsuroro/Library/Application Support/JetBrains/Toolbox/apps/datagrip/ch-0/212.4746.86/DataGrip.app/Contents/MacOS/datagrip" $wait --args "${ideargs[@]}"